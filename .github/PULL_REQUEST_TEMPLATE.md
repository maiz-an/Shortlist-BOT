## What and why

## Checklist
- [ ] `npm run typecheck` and `npm test` pass in `backend`
- [ ] `npm run build` passes in `frontend`
- [ ] Tests added or updated for backend logic
- [ ] No secrets or personal data in the diff
- [ ] If user-visible: `CHANGELOG.md` updated (and `VERSION` plus both `package.json` files if this is a release)
