# Leave empty: the dev server proxies /api to the backend, so any device can use the app.
VITE_API_BASE_URL=
# Local-only shared secret (filled in by `npm run init-env`). Not used in remote mode.
VITE_API_TOKEN=
